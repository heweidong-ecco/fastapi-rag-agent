# 重构计划 · 2026-09-15（当前最高优先级）

> **状态**：已获业务方批准（2026-09-15）。**本计划为当前最高优先级 —— 其余 M5 裁决工作让位于它。**
> **登记位置**（业务方要求"必须最高位，后续其他来看项目必须知道"）：
> ① `CLAUDE.md` 顶部（每会话自动加载，但该文件不入库、仅本机生效）· ② **本文件**（正文进公开仓库）· ③ `ROADMAP.md`「当前指针」最上方
> **跨会话入口**：`ROADMAP.md` → 本文件。

---

## Context：业务方口径 + 一条必须更正的前提

业务方对「重构」的定义：

> **「把不能完成的先归档，或者注释掉，或者切到单独模块中，之后再复现测试。」**
> 并判断「里面大部分代码都是可以测试的，只有小部分测试不了」。

### ⚠️ 前提级更正（写在这里是为了让下一个会话不再重踩）

> 我实测到的是「`docker build` 装**完整** `requirements.txt`（含 torch）会打穿 8GB 内存」，
> 却推出了「**这台机器做不了任何运行期验证**」。
> **前者是事实，后者是我没验证过的跳跃。**
> 而 `CLAUDE.md` 里早就写着「测试专用 venv（**在本仓库之外**）」—— **我读到了却没去找。**

**真实情况（实测）**：`/Users/heweidong/Desktop/ai-learning/venv`（Python **3.10.10**）里 20+ 关键包**一个不缺**：

```
fastapi · pydantic · uvicorn · pytest · httpx · starlette
langchain · langchain_openai · langchain_community · langgraph
psycopg2 · redis · gradio · matplotlib · mem0 · locust · playwright · mcp
numpy 1.26.4 · jwt · loguru · sqlalchemy · jieba · rank_bm25
openai · prometheus_client · dotenv
```

唯一缺 `torch / transformers / sentence-transformers` —— **本仓不需要**（`api/reranker.py:11-20` 把 `sentence_transformers` 放在 `get_reranker()` 内**真懒加载**）。

⇒ **`import main` 今天大概率就能跑。** 因此 **「切模块」从「解锁测试的前置条件」降级为「独立的代码质量目标」，不该排在 bug 前面。**

**环境事实**：Postgres(`localhost:5432`，容器 `postgres-rag`)、Redis(`localhost:6379`) 均健康；`rag_db` 的 5 张表已存在（70 docs / 319 logs / 1 api_key）。

### ⚠️ B3 实测：借来的那个 venv **跑不了 `import main`**（故改走「本仓自建」）

按要求先试了只读借用 `/Users/heweidong/Desktop/ai-learning/venv`：**B1 通过**（`MISSING: none`）、**B2 通过**，但 **B3 失败**：

```
api/api_v1.py:53    router = APIRouter(prefix="/api/v1")   ← 只传了 prefix
starlette/routing.py:837
TypeError: Router.__init__() got an unexpected keyword argument 'on_startup'
```

根因：**该 venv 的 fastapi / starlette 版本对不上** ——

| | 版本 | 事实 |
|---|---|---|
| fastapi | 0.115.11 | `APIRouter.__init__` **有** `on_startup`，并向下传给 starlette 的 `Router` |
| starlette | **1.6.0** | `Router.__init__` 参数只剩 `[routes, redirect_slashes, default, lifespan, middleware, max_body_size]` —— **`on_startup` / `on_shutdown` 已被移除** |

⇒ **任何 `APIRouter(...)` 都建不起来**；且不止一处 —— 打掉第一个后，`include_router` 又在 `for handler in router.on_startup:` 上炸。**差的是一整层 API，不是小补丁**（试过运行时桥接，确认走不通）。
另一个候选 `miniconda3` base 的 fastapi/starlette **配对正常**，但**缺 9 个包**（`pytest psycopg2 redis matplotlib mem0 mcp jwt loguru prometheus_client`）。

**⇒ 业务方 2026-09-15 决定：在本仓新建隔离 venv（决策①）。**

### ✅ 但这次探查带回一条好消息：「大部分代码可测」在纯 Python 层**成立**

只借用那个 venv 时，**15 个模块链全部 import 成功**：

```
config · permission · jwt_handler · db · token_tracker · cache
tool_visualizer · tool_health · document_preprocessor · reranker
query_rewriter · embedding_client · schemas · exceptions · metrics
```

**凡是不经过 fastapi 的链，完全可用。** 被卡的只有「实例化 APIRouter」那一条（= `main.py` / `api_v1*.py`）—— 而自建 venv 正是为解这一条。

### 📄 随之新增：`api/requirements-test.txt`

从 `requirements.txt` **派生（只做减法，未加任何新包）**，剔除测试不需要的重依赖：
`sentence-transformers`（拖 torch）· `transformers` · `camelot-py[cv]` · `opencv-python` · `ragas` · `datasets` · `locust`。
剔除依据：**`api/` 下顶层 import 命中 0 次**（`sentence_transformers` 那 2 处是函数内懒加载：`reranker.py:14`、`document_preprocessor.py:171` 且带 try/except）。
**代价已登记进清单③-a**：`mode=accurate/full` 与 `rerank_search()` 在本环境跑不了（默认 `accurate_norerank` 不碰 torch）。

### 已拍板的决策

| # | 决策 |
|---|---|
| ① | **在本仓新建隔离 venv**（`venv/`，Python 3.10.10），按 **`api/requirements-test.txt`** 安装 —— **不借用他人环境、不装 torch 系重依赖**（原因见下方 B3 实测） |
| ② | 测试用**新建的 `rag_test` 库**跑全套（`rag_db` 是 `agent-eval-gate` 评测的知识库，不可污染） |
| ③ | 归档用 **`git rm`**（git 历史即归档；`archive/` 不入库，挪进去等于从 git 消失） |
| ④ | #2 的验证用 **monkeypatch，零写库** |
| ⑤ | #3 的验证用**离线 OpenAPI 断言**（零外网、零 LLM 成本） |

---

## 〇、环境准备（B 之前，一次性）

```bash
cd /Users/heweidong/Desktop/Product/fastapi-rag-agent
python3.10 -m venv venv                                    # 本仓内隔离环境（.gitignore:2 已覆盖）
venv/bin/pip install -U pip
venv/bin/pip install -r api/requirements-test.txt          # 剔重版，**不含 torch 系**
```

**不装任何东西也可以先做 B1/B2** —— 那两个只用解释器自带能力。

---

## 一、基线（B0–B7）—— 绝对第一步

> 为什么第一：下面所有"哪些能测"目前都是**读码推断**，基线把它换成**实测数据**。
> ⚠️ 两个坑：**① 所有命令必须先 `cd api`**（`logs/`、`.mem0/` 都是相对 cwd）；**② `git status` 抓不到副产物**（全被 gitignore 且目录已存在）—— 改用 `find`+mtime 对比。
> ⚠️ **`compileall` 会写 `.pyc`**（`PYTHONDONTWRITEBYTECODE=1` 对它**无效** —— 写字节码就是它的本职）。所以 B2 之后**会多出 `__pycache__` 目录**，收尾时按「先列命中集再删」的纪律清理。

| # | 动作 | 命令要点 | 预期 / 失败含义 |
|---|---|---|---|
| **B0** | 记录指纹（零副作用） | `git rev-parse --short HEAD` + `find api/.mem0 api/logs api/.pytest_cache -type f -exec stat -f '%Sm %z %N'` | 作为 B7 对比锚点 |
| **B1** | 解释器自检 | `importlib.util.find_spec` 逐个查包 | `MISSING: none` |
| **B2** | 语法基线（复刻 CI） | `-m compileall api/ -q` | `SYNTAX OK` |
| **B3** | **`import main`** ⚠️ 首个有副作用的命令 | `cd api && $V -c "import main; print(len(main.app.routes))"` | 见下矩阵 |
| **B4** | pytest 收集 | `-m pytest --collect-only -q -p no:cacheprovider` | **预期 18 个**（`test_preprocess.py` 是脚本式，0 个） |
| **B5** | 真跑 pytest | 见「二」 | 首次结果即真基线 |
| **B6** | 起服务（可选） | `-m uvicorn main:app` + `curl /api/v1/ /health /ready /dashboard` | `/ready` 有 10s 门 |
| **B7** | 收尾对比 | 重跑 B0 的 `find` | 只应新增 `api/logs/api_*.log`、`.mem0/qdrant/.lock` mtime 变 |

**B3 结果解读矩阵**：

| 观察 | 含义 | 下一步 |
|---|---|---|
| `IMPORT OK` | 模块级副作用可承受；torch 确实只在 `get_reranker()` 内 | 进 B4；**清单①大幅扩张、清单③缩减** |
| `ModuleNotFoundError: sentence_transformers` | **回归信号**：某处在模块级触发了 `rerank()` | 查 `hybrid_search.py:9` / `rag_pipeline.py:8` 的调用点 |
| `ImportError: cannot import name 'create_tool_calling_agent'`（`api_v1_rag.py:681`） | ⚠️ **本计划唯一未验证的点**：venv 里 langchain 0.3.30 与 langchain_classic 1.0.7 并存，符号可能位移 | 记录并单独处理 |
| `EnvironmentError: 以下必需的环境变量未设置` | `.env` 缺键 | 确认是从 `api/` 跑（`config.py:12-14` 加载 `../.env`） |
| 卡住 >60s | `memory_store.py:12` 初始化 qdrant，或 gradio 预下载字体 | 等待；必要时 `GRADIO_ANALYTICS_ENABLED=False` |

---

## 二、测试库隔离（决策②）

**为什么必须**：`test_documents.py` / `test_integration.py` 会把测试文档（如"紫色长颈鹿"）**真实插进 `rag_db.documents`** —— 那是评测用的知识库，插进去会**真实影响检索结果**。

```bash
V=/Users/heweidong/Desktop/Product/fastapi-rag-agent/venv/bin/python
# 1. 建库
docker exec postgres-rag psql -U postgres -c "CREATE DATABASE rag_test;"
# 2. 建表 —— ⚠️ conftest.py:8 的 TestClient(app) 没有 with ⇒ 不触发 lifespan ⇒ create_table() 不会自动跑
cd api && POSTGRES_DB=rag_test PYTHONDONTWRITEBYTECODE=1 $V -c "import db; db.create_table()"
# 3. 跑测试时带 POSTGRES_DB=rag_test（或临时改 .env —— 记得改回！）
```

---

## 三、三类分（①现在能验 ②切一刀能验 ③必须隔离）

> ⚠️ **本节已被 B0–B7 的实测结果覆盖** —— 见下方「实测结论」。原读码版保留在 git 历史里。

### ① 现在就能验证

**A 组 — 纯标准库**（系统 `python3` 即可，连 venv 都不用）：
`api/permission.py` · `api/tool_visualizer.py` · `api/tool_health.py`（只 import 不调 `run_health_check`）· `api/document_preprocessor.py`（依赖同目录 `cleanup_rules.json`，已存在）· `api/reranker.py`（不能调 `rerank`）· `api/test_preprocess.py`（**脚本式，0 个断言**）

**B 组 — 1 个轻量包**（venv 全有）：
`config.py` · `logger_config.py` · `exceptions.py` · `schemas.py` · `metrics.py` · `jwt_handler.py` · `cache.py` · `tool_cache.py` · `quota_limiter.py` · `rate_limiter.py` · `tools_with_cache.py` · `mcp_tool_factory.py` · alembic ×3

**C 组 — 待 B3 实测确认可升入①**：`main.py` · `api_v1.py` · `deps.py` · `db.py` · `hybrid_search.py` · `rag_pipeline.py` · `api_v1_rag.py`

### ② 切一刀就能验证（5 个切开点，**纯搬移，不改逻辑**）

| # | 位置 | 动作 | 收益 |
|---|---|---|---|
| 1 | `db.py:213-282` | 中部的 `sqlalchemy` 表定义 + `numpy/jieba/rank_bm25` → **平级新文件** `db_metadata.py` / `bm25_index.py` | `auth.py`/`deps.py` 依赖 5 包 → 1 包 |
| 2 | `token_tracker.py:10` 与 `:13` | **两行重复的** `from db import get_db` → 删一行，另一行移进函数（该文件已有 3 处惰性导入可照抄） | 678 行成本模块脱离 psycopg2 |
| 3 | `code_executor.py:12,65`、`simple_tools.py:4,8,20` | 纯逻辑抽成 `_impl`，`@tool` 留薄包装层 | 沙箱白名单变纯 stdlib |
| 4 | `main.py:503-507` | gradio 挂载包进 `if os.getenv("ENABLE_DASHBOARD","true")=="true":` | import 不再必拉 gradio+matplotlib |
| 5 | `api_v1_rag.py:688/731/732` | 模块级 `ChatOpenAI`/`create_tool_calling_agent`/`AgentExecutor` 搬进 `agent_websocket`（同文件 L527 已有函数内范例） | 不再 import 即建 LLM 对象 |

**做完 1+2，「纯 stdlib 可跑」从 6 个 → 约 11 个。**

### ③ 必须隔离 / 归档待复现

- **③-a 被 torch 挡住**（环境缺口，非代码问题）：`/rag/search?mode=accurate`、`?mode=full`、`hybrid_search.py:41` 的 `rerank_search()`。**默认 mode 是 `accurate_norerank` ⇒ 默认路径不碰 torch**
- **③-b 需外网 LLM / MCP 子进程**：3 套 Agent 图、`plan_execute.py`、`memory_store.py`、`query_rewriter.py`、`search_tools.py`、`embedding_client.py`、`browser_tools.py`
  ⚠️ **其中 `embedding_client.py:10` 的模块级 `OpenAI(api_key=DASHSCOPE_API_KEY)` 会在 key 为空时炸掉整条 import 链 —— 优先级最高**
- **③-c 已被取代的代际**（`docs/CODE_INVENTORY.md` §2）：3 代 Agent 里的 2 代、5 个检索端点里的 4 个、3 份 `calculator`、3 版 locustfile

**"隔离"的具体动作（决策③ = `git rm`）**：
- 真死代码 → `git rm`（git 历史即归档）
- **仍挂在线上但该被取代的** → **原地加一行标记**（不挪文件）：`# STATUS: superseded → 见 docs/CODE_INVENTORY.md §2 组N`
  （理由：`api/LEARNING_INDEX.md` 定义过一整套治理格式，**全仓 0 个文件使用** —— 再建新格式大概率同样烂掉）
- **"注释掉"只用于 ③-b 的模块级副作用，且用环境变量门控而非注释**（= 切开点 4/5）
- **真删的只有 `CODE_INVENTORY.md` §3-1 已判定的 16 行**：`rate_limiter.py:123-127`、`agent_graph_advanced_learning.py:216-219`、`api_v1_rag.py:577-579+611-614`

---

## 四、执行顺序

```
B0→B7 基线 ──▶ 用实测结果重画三类清单
                    │
   ①#1 口令 ──▶ ②N1 中间件路径 ──▶ ③#3 压测口径 ──▶ ④#2 预算单位
                    │
   ⑤归档(git rm) ← 可与 ①~④ 并行 ──▶ ⑥切模块(1→2→3→4→5 严格串行) ──▶ ⑦M6 单模块测试闭环
```

**bug 排在切模块之前**（原计划反了）：现在 bug **就能测**；`#1` 是公开仓库上的活口令，每多留一天都是净风险；且先切模块会把 bug diff 淹没在搬运 diff 里，**违反"一 PR 一逻辑变更"**，回滚粒度也会退化。

**⑥ 严格串行**：切开点 1 必须先于 2（2 的目标"不依赖 db 的重包"取决于 1 之后 `db` 还剩几个重包）。**切开点一个 commit 一个** —— 否则回滚粒度退化成"全部重来"。

---

## 五、四个 bug 的验证方式

> **执行进度**：✅ **① #1 认证口令**（2026-09-16 完成，见 `CHANGELOG.md` 的 `### Fixed`）· ⬜ ② N1 中间件路径 · ⬜ ③ #3 压测口径 · ⬜ ④ #2 预算单位

### ① #1 认证口令（路线 C）—— ✅ 已完成（2026-09-16）

改 `api/auth.py:87-89` 为**函数式读取**（不是模块级读一次，否则测试没法 monkeypatch）。**牵连 15 处**（实测 > 原估 11）：`auth.py:88,89` · `conftest.py:15` · `test_auth.py:6,38,53` · `test_integration.py:20` · `schemas.py:67` · 3 个 locustfile · Postman ×4 · `.env.example`

**验证**：`cd api && $V -m pytest test_auth.py test_integration.py -q`
⚠️ **最容易出的回归**：`test_login_failure`（错误口令）**必须仍然 401**，不能因环境变量缺失变成 500。

### ② N1 中间件路径（新发现，顺手修）

`main.py:106-109` 与 `191-193` 的跳过名单缺 `/api/v1` 前缀（真实路由在 `api_v1.py:59` 带前缀）⇒ **登录接口实际会打到 Redis**。
修法：两份重复名单提成模块级 `PUBLIC_PATHS` 常量。

**验证（零 Redis 依赖）**：
```python
def test_public_paths_cover_real_routes():
    from main import app, PUBLIC_PATHS
    routes = {r.path for r in app.routes}
    assert "/api/v1/auth/login" in routes
    assert "/api/v1/auth/login" in PUBLIC_PATHS   # 断言"名单 ∈ 真实路由"，改前缀也能抓到
```

### ③ #3 压测口径（**离线，零成本**）

改 `locustfile_hybrid.py:83/101/113` 的 `json=` → `params=`；`mode` 从 body 提到 query。

**验证 —— 断言"脚本发的形状" == "OpenAPI 声明的形状"**：
```python
def test_locust_payload_matches_openapi():
    from main import app
    spec = app.openapi()
    post = spec["paths"]["/api/v1/agent/mcp_chat"]["post"]
    assert "question" in {p["name"] for p in post.get("parameters", [])}
    assert "requestBody" not in post          # ← 证明 json= 必 422
    qs = {p["name"] for p in spec["paths"]["/api/v1/rag/search"]["post"]["parameters"]}
    assert "mode" in qs                        # ← 证明 body 里的 mode 被静默忽略
```
**零外网、零成本**，且以后写新 locustfile 时同一条测试能拦住同类错误。
（实机 `locust` 已装 2.44.4，但每次请求**真花钱** ⇒ 留给业务方决定，**不进 CI**。）

### ④ #2 预算单位（**monkeypatch，零写库**）

⚠️ **原计划的"插假数据"方案是错的，做不出来**：修**前**的代码读的是 `cost`（元），插多少 token 都区分不了红绿。

**正确做法**：只 patch `get_daily_token_usage`、**不** patch `get_daily_usage_cost`。

| | 修复前 | 修复后 |
|---|---|---|
| `check_budget_before_call` 调 | `get_daily_usage_cost`(505) | `get_daily_token_usage` |
| 返回 | **真走库** → ¥0.0002 | **被 patch** → 9999 |
| 判据 | `0.01 > 10000−0.0002`？否 | `est > 10000−9999`？**是** |
| 断言 | **FAIL（红）** | **PASS（绿）** |

**改动**：`token_tracker.py:505/625/667` 取数换 `get_daily_token_usage`；新增 `TOOL_ESTIMATED_TOKENS`/`PURPOSE_ESTIMATED_TOKENS`（**不删旧的 ¥ 表** —— `api_v1_agent.py:530` 对外暴露了 `estimate_tool_cost`）；抽 `check_token_budget_detail()` 消除三份重复实现；9 处 `¥` 文案改 tokens。
**注意**：`api_v1_agent.py:321` 的 `check_budget` → `check_token_budget`（`token_tracker.py:288`）**本来就是对的**，不要动。

---

## 六、风险与回滚

| 风险 | 防 |
|---|---|
| **R1 循环导入**（拆 `db.py` 后） | 新模块**只单向** `from db import get_db`；`db.py` 的 re-export 用函数内惰性导入；验收用一次全链路 `import db, bm25_index, auth, deps, api_v1` |
| **R2 惰性导入失效** | 惰性模块各配一条**冒烟测试**（真调一次，不是 import）；验收写成"调用成功"而非"导入成功" |
| **R3 不能建子包** | `api/` 下**无任何 `__init__.py`**，全靠 sys.path 扁平导入 ⇒ 切开点全部产**平级新文件**，不建子目录 |
| **R4 re-export 让 patch 失效** | 不用 `import *`；先 grep 现有 `patch("db.` |
| **R5 门控默认值写反** | `main.py:507` 是 `app = gr.mount_gradio_app(app, ...)`，**它重绑定 `app`** ⇒ 默认取 `"true"` 保持现状；加测试断言 `/dashboard` 路由存在；**门 2 的 `len(app.routes)` 做数字对比** |
| **R6 测试污染真实库** | 用 `rag_test` 库（见「二」） |

**回滚**：①~④ 各一个 commit；⑤ `git rm` 可 `git revert`（历史在）；⑥ **每个切开点一个 commit**；⑦ 只加测试文件。

**每次改完的验收门**：
```bash
V=/Users/heweidong/Desktop/Product/fastapi-rag-agent/venv/bin/python
PYTHONDONTWRITEBYTECODE=1 $V -m compileall api/ -q                                        # 门1 语法（复刻 CI）
cd api && PYTHONDONTWRITEBYTECODE=1 $V -c "import main; print('OK', len(main.app.routes))"  # 门2 全链路导入 + 路由数对比
cd api && PYTHONDONTWRITEBYTECODE=1 $V -m pytest -q -p no:cacheprovider                     # 门3 对比 B5 基线
```

---

## 七、明确「不做」

| 不做 | 理由 |
|---|---|
| 装 `torch`/`transformers`/`sentence-transformers` | 业务方明确要求（8GB）；仓库也不需要。**代价**：`mode=accurate/full` 与 `rerank_search` 永久留在清单③-a —— **要写明，别让它变成"忘了"** |
| **任何 `pip install`** | venv 已覆盖全部所需。**要装之前先问** |
| 跑 `rerank()` / `rerank_async()` | 触发 2.3GB 模型下载 |
| 构建/推 Docker 镜像 | `ci.yml:3-5` 明说 CI 故意与重构无关；且 `docker-compose.yml` 当前是 modified 状态 |
| 改 CI 加 pytest | `ci.yml:4` 明说待 M6 后再议（**但基线跑通后应重新提上议程**） |
| 碰 `archive/` 里已有的脚本 | 不入库，不在任何活路径上 |
| 修 `CODE_INVENTORY.md` §2 的 8 组代际并存 | 本次只做「**标记 + 归档**」，**不做「裁决 + 合并」**（那是 M5 的 C/D/E 档，5–8 天） |
| 动 `docker-compose.yml` | 它当前已是 modified 未提交 —— 先问清楚那个改动，别在脏文件上叠改动 |
| PR 自合 | `ROADMAP.md`：开好 PR 要先问「可以合吗」，**Agent 自开的 PR 不许自合** |
| 跑 `locust` 打真实服务 | 真花钱。只做离线 OpenAPI 验证 |
| 用系统 `python3`（3.14.7） | 无任何第三方包，必失败 |

---

## 八、不确定项（未猜测）

1. **`api_v1_rag.py:681` 的 `from langchain.agents import create_tool_calling_agent, AgentExecutor`** —— `find_spec('langchain.agents')` 通过，但**符号级导入未验**（验它就要 `import main`，有副作用）。这是 B3 唯一可能失败的点。
2. **`conftest.py:8` 不触发 lifespan** —— 因 5 张表已存在而被短路。这意味着 **B5 是"环境依赖型通过"**，换台机器可能红。
3. **`locust` 的 import** 曾报 `RuntimeError: greenlet is being finalized`（`find_spec` 通过）—— 可能是 gevent+shell 环境干扰，走 `python -m locust` 未必复现。
4. **gradio 版本未查** —— 切开点 4 要注意 `mount_gradio_app` **返回新 app**（`main.py:507` 的 `app = ...` 是证据）。

---

## 九、待业务方后续裁决

- **`#1` 是否分两个 commit**（先带过渡默认值，再删）—— 避免"改凭据 ⇒ 所有测试中途全红"的窗口
- **CI 是否加 pytest**（基线跑通后再议；且需要 Postgres+Redis service，不是加一行的事）

---

## 十、B0–B7 实测结果（2026-09-15 完成）✅

| # | 检查 | 结果 |
|---|---|---|
| B0 | 指纹记录 | ✅ 25 个副产物文件（`api/logs/` `api/.mem0/` `api/.pytest_cache/`） |
| B1 | 解释器（**本仓自建 venv**） | ✅ Python 3.10.10，`MISSING: none` |
| B2 | 语法（复刻 CI） | ✅ `SYNTAX OK` |
| **B3** | **`import main`** | ✅ **成功** —— OpenAPI **59 条路径**（`/agent` 30 · `/rag` 15 · `/auth` 2 · 其余健康/调试） |
| **B4** | pytest 收集 | ✅ **18 个**（与预判完全一致） |
| **B5** | **真跑 pytest** | ✅ **`17 passed, 1 skipped`，4.33s** —— **与 `CLAUDE.md:19` / `GIT_CHECKLIST.md:19` 记录的期望值完全一致** |
| B6 | 起服务 | ⬜ 未做（可选，有副作用） |
| B7 | 副产物对比 | ✅ 只新增 `api/logs/api_2026-09-15.log`(2612B) 与 `error_2026-09-15.log`(0B)；**根目录无误建**；`__pycache__` 0 个 |

**⇒ 「大部分代码可测」被实测证实 —— 不只是可测，是 17 个用例全绿。**

⚠️ **两个度量陷阱（记录备查）**：
1. **`len(app.routes)` 不再等于路由总数** —— FastAPI 0.141 把 `include_router` 的结果包成了 `_IncludedRouter` 条目（3 个条目承载 57 条子路由），所以 `app.routes` 只有 14。**权威指标是 `len(app.openapi()["paths"])`**。
2. **`import main` 耗时 ~92 秒** —— 大头是 gradio 面板与 mem0/qdrant 的**模块级初始化**。跑任何 pytest 都要付这个固定成本。

### 🔴 顺带查出 `api/requirements.txt` 的**三个真实缺陷**（都是"无上界/改包名"）

装上最新版就会在 import 阶段崩，**今天重跑 `docker build` 出来的镜像应用根本起不来** ——
这解释了为什么那张 2026-07-01 的镜像"不能随便重建"。

| # | 缺陷 | 症状 | 修法 |
|---|---|---|---|
| 1 | `langchain>=0.3.13` **无上界** | pip 装到 **langchain 1.x**；`api_v1_rag.py:681` `from langchain.agents import create_tool_calling_agent, AgentExecutor` → **ImportError**（1.x 已移到 `langchain_classic`） | 加 `<0.4`（`langchain` / `-core` / `-openai` / `-community` / `-text-splitters` 全系列） |
| 2 | `duckduckgo-search>=6.0.0` **包已改名** | 新版 `langchain_community` 要的是 **`ddgs`**；`api/agent_graph.py:43` **模块级**调 `DuckDuckGoSearchRun()` → **ImportError** | 换成 `ddgs>=9.0.0` |
| 3 | `mcp>=1.0.0` **无上界** | pip 装到 **mcp 2.2.0**；`api/mcp_server.py:49` `@server.list_tools()` → **AttributeError**（2.x 去掉了该方法） | 加 `<2` |

> **✅ 已修（2026-09-15 业务方裁决：插队，先于三个 bug 修）** —— `api/requirements.txt` 与 `api/requirements-test.txt` **两个文件都改**。
>
> **改动**：① langchain 全系列加上界（`<0.4` / `-openai <0.3` / `-core <0.4` / `-community <0.4` / `-text-splitters <0.4` / `langgraph <2` / `openai <4`）② `duckduckgo-search>=6.0.0` → **`ddgs>=9.0.0`** ③ `mcp>=1.0.0` → **`>=1.0.0,<2`** ④ 顺带删掉两处**裸名重复条目**（`langgraph` / `langchain-core` 在文件里各出现两次，其中一次无约束 —— 重复条目会掩盖版本漂移）。
>
> **验证（三项都过）**：
> - `pip install --dry-run -r api/requirements.txt` **完整解析成功**（含 torch/sentence-transformers/ragas/camelot/locust，无冲突）
> - 两个依赖文件**共享 39 个包、约束口径 0 差异、均无重复条目**
> - 三道验收门：门1 语法 ✅（57 文件）· 门2 全链路导入 ✅（OpenAPI 59 路径）· 门3 pytest ✅ **17 passed, 1 skipped**
>
> ⇒ **`docker build` 从此可重建**（此前装到 langchain 1.x / mcp 2.x 会在 import 阶段就崩）。

### ✅ 顺带验证：#3 的假设**已被应用自己的 OpenAPI schema 证实**

```
/agent/mcp_chat  parameters = ['question','thread_id','memory_space','x-api-key']
                 有 requestBody 吗: False        ← 发 json body 必 422 ✅
/rag/search      query 参数 = ['mode','x-api-key'] ← mode 确实是 query，body 里的被静默忽略 ✅
```

⇒ #3 的离线验证手法（断言"脚本形状 == OpenAPI 声明"）**可行且已跑通**，不再是静态推断。

---

## 变更记录
- 2026-09-15 建立。经业务方批准，登记为项目最高优先级（三处：`CLAUDE.md` 顶部 / 本文件 / `ROADMAP.md` 当前指针最上方）。
