# Web 框架与服务器
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# 环境变量
python-dotenv>=1.0.0

# 大模型与 AI 框架
# ⚠️ 必须加上界：本仓代码是按 langchain **0.3.x** 写的。原先只写 `>=0.3.13`（无上界），
#    pip 会装到 **1.x**，而 1.x 把 classic agent 移到了 `langchain_classic`，导致
#    api/api_v1_rag.py:681 `from langchain.agents import create_tool_calling_agent, AgentExecutor`
#    → ImportError: cannot import name 'create_tool_calling_agent' from 'langchain.agents'
#    ⇒ 应用在 **import 阶段**就崩（2026-09-15 实测）。
openai>=1.0.0,<4
langchain>=0.3.13,<0.4
langchain-openai>=0.2.14,<0.3
langchain-community>=0.3.13,<0.4
langchain-core>=0.3.0,<0.4
langchain-text-splitters>=0.3.0,<0.4
langgraph>=1.0,<2

# 文档处理
pymupdf>=1.24.0
docx2txt>=0.8

# 工具与搜索
# ⚠️ `duckduckgo-search` 已**改名为 `ddgs`**，新版 langchain_community 要的是后者。
#    旧名留着会导致 api/agent_graph.py:43（**模块级**调 DuckDuckGoSearchRun()）
#    → ImportError: Could not import ddgs python package（2026-09-15 实测）。
ddgs>=9.0.0

# 数据库
psycopg2-binary>=2.9.0

# 缓存
redis>=5.0.0

# 认证
pyjwt>=2.8.0

# 日志
loguru>=0.7.0

# 监控
prometheus-client>=0.20.0

# WebSocket
websockets>=12.0

# 测试与压测（后续用到）
httpx>=0.27.0
pytest>=8.0.0
locust>=2.30.0

# RAG深度优化
rank-bm25>=0.2.2
jieba>=0.42.1
sentence-transformers>=3.0.0
# 重排序兼容性约束：transformers 5.x 与 torch 2.2 不兼容（已实测）
transformers>=4.41,<5
numpy>=1.24,<2
ragas>=0.1.18

# RAG
pdfplumber>=0.11.0
camelot-py[cv]>=0.11.0
opencv-python>=4.8.0
python-docx>=1.1.0
markdown>=3.7.0
beautifulsoup4>=4.12.0
lxml>=5.3.0
python-multipart>=0.0.9

# Agent langGraph
# 注：langgraph 与 langchain-core 已在上方「大模型与 AI 框架」段声明**带约束**的版本，
#     此处原先又裸名重复了一次（无约束）。重复条目会掩盖版本漂移，已删除。
langgraph-checkpoint
langgraph-checkpoint-sqlite
# mem0 2.x 起 Memory.__init__ 不再接受 dict，代码使用 Memory.from_config(dict)，需 >=2.0
mem0ai>=2.0.0

# Gradio
matplotlib
# 工具
playwright>=1.48.0

# MCP Server
# ⚠️ 必须加上界：mcp 2.x 的 `Server` 去掉了 `list_tools()` 方法，而
#    api/mcp_server.py:49 用的是 `@server.list_tools()` → AttributeError（2026-09-15 实测）。
mcp>=1.0.0,<2

# 成本可视化面板（main.py 挂载 Gradio）
gradio>=4.0.0

# Alembic / 元数据定义（db.py 使用 sqlalchemy）
sqlalchemy>=2.0.0

# RAGAS 评估脚本依赖 datasets
datasets>=2.0.0
